spring:
  application:
    name: MICE STAR

  datasource:
    url: jdbc:mysql://localhost:3306/micestar
    username: root
    password: krbh4059
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
    show-sql: true


logging:
  level:
    org.hibernate.sql: debug
    org.springframework.boot.autoconfigure: error